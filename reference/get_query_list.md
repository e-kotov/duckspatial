# Get names for the query

Get names for the query

## Usage

``` r
get_query_list(x, conn)
```

## Arguments

- x:

  sf or character

- conn:

  A connection object to a DuckDB database. If `NULL`, the function runs
  on a temporary DuckDB database.

## Value

list with fixed names
